# dependencies
node_modules/
package-lock.json

# os / editor
.DS_Store
*.log

# large / generated assets (keep source out of git; optimize before committing)
*.raw.glb
scratchpad/

# per-machine hook state (turn counter for the tracker nudge)
.claude/.tracker-state.json
